var firstNonRepeatedCharacter = function (string) {
/*     
    let a = 0
    let b = 1
    let c = 2
    let d = 3
    let previousLetter = ''
    let firstLetter = string.slice(a,b);
    let nextLetter = string.slice(b,c);
    let thirdLetter = string.slice(c,d);
    let remainingLetters = string.slice(1,string.length)
    

    while (remainingLetters.length > 0) {
        while (firstLetter == nextLetter || previousLetter ==  ) {
            a++
            b++
            c++
            d++
            previousLetter = firstLetter;
            firstLetter = string.slice(a,b);
            nextLetter = string.slice(b,c);
            thirdLetter = string.slice(c,d)
            remainingLetters = string.slice(b,string.length)

        }
        if (firstLetter != nextLetter)
        {
            return nextLetter
        }
            
        

    } */
  
  };
console.log(firstNonRepeatedCharacter('aabbdcc'))


  